<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:assist="clr-namespace:Utilidades.Assist"
                    xmlns:converter="clr-namespace:Utilidades.Converters">

    <converter:TextMultiConverter x:Key="TextMultiConverter"/>
    
    <!-- Label -->
    <Style x:Key="FieldLabel" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{DynamicResource MaterialDesignBodyLight}" />
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="Margin" Value="0,10,0,0"/>
    </Style>

    <!-- Value -->
    <Style x:Key="FieldValue" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{DynamicResource MaterialDesignBody}" />
        <Setter Property="FontSize" Value="15"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
    </Style>

    <Style x:Key="FieldStyle" TargetType="ContentControl">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ContentControl">
                    <StackPanel>

                        <!-- Label -->
                        <TextBlock 
                        Text="{Binding 
                            RelativeSource={RelativeSource TemplatedParent}, 
                            Path=(assist:FieldAssist.Label)}"
                        Style="{StaticResource FieldLabel}" />

                        <!-- Value -->
                        <TextBlock Style="{StaticResource FieldValue}">
                            <TextBlock.Text>
                                <MultiBinding Converter="{StaticResource TextMultiConverter}">
                                    <Binding RelativeSource="{RelativeSource TemplatedParent}" Path="Content"/>
                                    <Binding RelativeSource="{RelativeSource TemplatedParent}" Path="(assist:FieldAssist.Format)"/>
                                </MultiBinding>
                            </TextBlock.Text>
                        </TextBlock>

                    </StackPanel>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>