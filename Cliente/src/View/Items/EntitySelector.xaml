<UserControl x:Class="Cliente.View.Items.EntitySelector"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
             x:Name="Root">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <TextBox Grid.Column="0"
                 IsReadOnly="True"
                 Text="{Binding DisplayText, ElementName=Root, Mode=OneWay}"
                 md:HintAssist.Hint="{Binding HitText, ElementName=Root}"
                 Style="{StaticResource MaterialDesignOutlinedTextBox}"/>

        <Button Grid.Column="1"
                Command="{Binding OpenDialogCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                Style="{StaticResource MaterialDesignIconButton}"
                VerticalAlignment="Bottom"
                Margin="4,0,0,0">
            <md:PackIcon Kind="Magnify"/>
        </Button>
    </Grid>
</UserControl>