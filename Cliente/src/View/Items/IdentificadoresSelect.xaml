<UserControl x:Class="Cliente.View.Items.IdentificadoresSelect"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:extencions="clr-namespace:Cliente.Extencions"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <extencions:BindingProxy x:Key="Proxy" Data="{Binding RelativeSource={RelativeSource AncestorType=UserControl}}"/>

    </UserControl.Resources>
    <ScrollViewer HorizontalScrollBarVisibility="Auto">
        <StackPanel Margin="10">
            <ItemsControl ItemsSource="{Binding IdentificadoresOrigen, RelativeSource={RelativeSource AncestorType=UserControl}}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Margin="10">
                            <TextBlock Text="{Binding Name}" FontWeight="Bold" HorizontalAlignment="Center"/>
                            <ComboBox ItemsSource="{Binding Valores}"
                                      SelectedItem="{Binding ValorSeleccionado, Mode=TwoWay}"
                                      MinWidth="120" Margin="0,5">
                                <ComboBox.ItemTemplate>
                                    <DataTemplate>
                                        <Border>
                                            <Grid>
                                                <TextBlock Text="{Binding Value}" VerticalAlignment="Center" />
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ComboBox.ItemTemplate>
                                <ComboBox.ItemContainerStyle>
                                    <Style TargetType="ComboBoxItem" BasedOn="{StaticResource MaterialDesignComboBoxItemStyle}">
                                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                        <Setter Property="VerticalContentAlignment" Value="Stretch" />
                                        <Setter Property="ContextMenu">
                                            <Setter.Value>
                                                <ContextMenu>
                                                    <MenuItem Header="Editar"
                                                              Command="{Binding Data.EditarCommand, Source={StaticResource Proxy}}"
                                                              CommandParameter="{Binding}">
                                                        <MenuItem.Icon>
                                                            <md:PackIcon Kind="Pencil" Width="18" Height="18" />
                                                        </MenuItem.Icon>
                                                    </MenuItem>

                                                    <MenuItem Header="Eliminar"
                                                              Command="{Binding Data.EliminarCommand, Source={StaticResource Proxy}}"
                                                              CommandParameter="{Binding}">
                                                        <MenuItem.Icon>
                                                            <md:PackIcon Kind="Delete" Width="18" Height="18" />
                                                        </MenuItem.Icon>
                                                    </MenuItem>
                                                </ContextMenu>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </ComboBox.ItemContainerStyle>
                            </ComboBox>
                            <Button Content="+ Add"
                                    Width="80"
                                    Command="{Binding .AgregarCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                    CommandParameter="{Binding}" />
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
    </ScrollViewer>
</UserControl>
