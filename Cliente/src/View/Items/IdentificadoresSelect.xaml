<UserControl x:Class="Cliente.View.Items.IdentificadoresSelect"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:extencions="clr-namespace:Cliente.Extencions"
             xmlns:converter="clr-namespace:Cliente.Converter"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
    </UserControl.Resources>
    <ScrollViewer HorizontalScrollBarVisibility="Auto">
        <StackPanel Margin="10">
            <ItemsControl
                ItemsSource="{Binding RelativeSource={RelativeSource AncestorType=UserControl}, Path=Selectores}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Margin="10">
                            <ComboBox ItemsSource="{Binding Valores}"
                                      md:HintAssist.Hint="{Binding Identificador.Name}"
                                      FontSize="15"
                                      SelectedItem="{Binding Seleccionado, Mode=TwoWay}"
                                      MinWidth="120" Margin="0,5"
                                      DisplayMemberPath="Valor" />

                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>


        </StackPanel>
    </ScrollViewer>
</UserControl>