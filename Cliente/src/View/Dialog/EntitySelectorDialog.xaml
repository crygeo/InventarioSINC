<UserControl x:Class="Cliente.View.Dialog.EntitySelectorDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:Cliente.View.Items"
             xmlns:util="clr-namespace:Utilidades.Converters;assembly=Utilidades"
             MinWidth="550" MinHeight="420" MaxHeight="600">

    <UserControl.Resources>
        <util:InverseBooleanConverter x:Key="InverseBoolToVisConverter" />
        <util:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </UserControl.Resources>
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0"
                   Text="{Binding TextHeader, RelativeSource={RelativeSource AncestorType=UserControl}}"
                   Style="{StaticResource MaterialDesignHeadline6TextBlock}"
                   Margin="0,0,0,16" />

        <!-- Buscador -->
        <Grid Grid.Row="1" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBox Grid.Column="0"
                     md:HintAssist.Hint="Buscar..."
                     Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                     Style="{StaticResource MaterialDesignOutlinedTextBox}">
                <TextBox.InputBindings>
                    <KeyBinding Key="Enter" Command="{Binding SearchCommand}" />
                </TextBox.InputBindings>
            </TextBox>

            <Button Grid.Column="1"
                    Command="{Binding SearchCommand}"
                    Style="{StaticResource MaterialDesignIconButton}"
                    Margin="8,0,0,0"
                    ToolTip="Buscar">
                <md:PackIcon Kind="Magnify" />
            </Button>
        </Grid>

        <!-- Resultados -->
        <Grid Grid.Row="2">
            <ProgressBar IsIndeterminate="True"
                         VerticalAlignment="Top"
                         Visibility="{Binding IsLoading,
                                      Converter={StaticResource BooleanToVisibilityConverter}}" />

            <DataGrid x:Name="ResultsGrid"
                      ItemsSource="{Binding Results}"
                      SelectedItem="{Binding SelectedItem, Mode=TwoWay}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      SelectionMode="Single"
                      CanUserAddRows="False"
                      Visibility="{Binding IsLoading, 
                                   Converter={StaticResource InverseBoolToVisConverter}}">
                <DataGrid.InputBindings>
                    <!-- Doble clic = confirmar -->
                    <MouseBinding MouseAction="LeftDoubleClick"
                                  Command="{Binding AceptarCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                  CommandParameter="{Binding SelectedItem}" />
                </DataGrid.InputBindings>
            </DataGrid>
        </Grid>

        <!-- Botones -->
        <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,16,0,0">

            <Button Content="CANCELAR"
                    Style="{StaticResource MaterialDesignFlatButton}"
                    Command="{x:Static md:DialogHost.CloseDialogCommand}"
                    CommandParameter="{x:Null}"
                    Margin="0,0,8,0" />

            <Button Content="ACEPTAR"
                    Style="{StaticResource MaterialDesignRaisedButton}"
                    IsEnabled="{Binding CanConfirm}"
                    Command="{Binding AceptarCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                    CommandParameter="{Binding SelectedItem}" />
        </StackPanel>
    </Grid>
</UserControl>