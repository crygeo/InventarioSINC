<UserControl x:Class="Cliente.View.Dialog.ProveedorDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800">

    <md:DialogHost
        Identifier="{Binding DialogNameIdentifier, RelativeSource={RelativeSource AncestorType=UserControl}}">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock Text="{Binding TextHeader, RelativeSource={RelativeSource AncestorType=UserControl}}"
                       Margin="10" />
            <!-- Título y switch -->
            <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,0,0,10">
                <TextBlock Text="Es Empresa:" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,10,0" />
                <ToggleButton
                    IsChecked="{Binding Entity.EsEmpresa, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=TwoWay}"
                    Style="{StaticResource MaterialDesignSwitchToggleButton}" />

            </StackPanel>

            <!-- Contenido dinámico -->
            <Grid Grid.Row="2">

                <StackPanel x:Name="PanelGeneral" />
            </Grid>

            <!-- Botones -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Grid.Row="3" Margin="0,20,0,0">
                <Button Content="Cancelar" Click="OnCancelarClick" Margin="5" />
                <Button Content="Aceptar" Click="OnAceptedClick" Margin="5" />
            </StackPanel>
        </Grid>
    </md:DialogHost>
</UserControl>
<!-- End of ProveedorDialog.xaml -->