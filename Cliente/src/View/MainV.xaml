<Window x:Class="InventarioSINCliente.src.View.MainV"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:InventarioSINCliente.src.View"
        xmlns:viewModel="clr-namespace:InventarioSINCliente.src.ViewModel"
        mc:Ignorable="d"
        Title="MainV" Height="450" Width="800"
    xmlns:converters="clr-namespace:MaterialDesignThemes.Wpf.Converters;assembly=MaterialDesignThemes.Wpf"
    xmlns:wpf="clr-namespace:MaterialDesignThemes.Wpf;assembly=MaterialDesignThemes.Wpf">
    <Window.Resources>
        <viewModel:MainVM x:Key="viewModel"/>

    </Window.Resources>
    <Grid DataContext="{Binding Source={StaticResource viewModel}}">
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Background="{StaticResource MainView_Background_1}" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Orientation="Horizontal">
            <RadioButton Style="{DynamicResource RadioButtonStyle1}" Command="{Binding ChangePageCommand}" CommandParameter="Usuarios" GroupName="Pages" IsChecked="True" Content="Usuarios" VerticalContentAlignment="Center"  VerticalAlignment="Stretch" />
            <RadioButton Style="{DynamicResource RadioButtonStyle1}" Command="{Binding ChangePageCommand}" CommandParameter="Roles" GroupName="Pages" Content="Roles" VerticalContentAlignment="Center" VerticalAlignment="Stretch" />
        </StackPanel>
        <Grid Grid.Row="1" Background="{StaticResource MainView_Background_2}">
            <Frame Content="{Binding PageSelectViewModel}" NavigationUIVisibility="Hidden"/>
        </Grid>
    </Grid>
</Window>
