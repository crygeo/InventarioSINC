<UserControl x:Class="Cliente.View.Model.PageValoresV"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Cliente.View.Model"
             xmlns:model="clr-namespace:Cliente.src.ViewModel.Model"
             xmlns:converter="clr-namespace:Cliente.src.Converter"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" d:DataContext="{d:DesignInstance model:PageValoresVM}">

    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header Section -->
        <materialDesign:DrawerHost>
            <materialDesign:DrawerHost.LeftDrawerContent>
                <ListBox
                    ItemsSource="{Binding Navigation}"
                    SelectedItem="{Binding NavigationSelect}"
                    BorderThickness="0"
                    Background="Transparent">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="{Binding SelectedIcon}" Width="24" Height="24" />
                                <TextBlock Text="{Binding Title}" Margin="10,0,0,0" />
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </materialDesign:DrawerHost.LeftDrawerContent>

            <Grid>
                <!-- BOTÓN HAMBURGUESA -->
                <Button 
                        HorizontalAlignment="Left"
                        VerticalAlignment="Top"
                        Margin="0,0"
                        Padding="6"
                        Command="{x:Static materialDesign:DrawerHost.OpenDrawerCommand}">
                    <materialDesign:PackIcon Kind="Menu" Width="24" Height="24"/>
                </Button>

                <!-- VISTA PRINCIPAL -->
                <ContentControl Margin="50,0,0,30" Content="{Binding NavigationSelect.Page}" ContentTemplateSelector="{StaticResource ViewModelTemplateSelector}"/>
            </Grid>

        </materialDesign:DrawerHost>

    </Grid>
</UserControl>
